# Nixpacks configuration for Railway deployment
# Forces Node.js 20

[phases.setup]
nixPkgs = ['nodejs_20']

[phases.build]
cmds = ['cd bot && npm ci']

[phases.start]
cmds = ['cd bot && npm start']

[start]
cmd = 'cd bot && npm start'
